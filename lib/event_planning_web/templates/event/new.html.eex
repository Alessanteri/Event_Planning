<h1>New Event</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_event_path(@conn, :create, @user)) %>

<span><%= link "Back", to: Routes.user_event_path(@conn, :my_schedule, @user) %></span>

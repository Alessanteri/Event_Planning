<h1>Edit Event</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_event_path(@conn, :update, @user, @event)) %>

<span><%= link "Back", to: Routes.user_event_path(@conn, :show, @user, @event) %></span>
